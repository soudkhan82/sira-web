import Link from "next/link"; import type { SiraIncident } from "@/types/incident"; import { formatDateShort,getPublicIncidentCode,getShortStatusLabel } from "@/types/incident"; import { inferSiraArea } from "@/lib/siraAreas";
export default function IncidentRow({i}:{i:SiraIncident}){const area=inferSiraArea(i);return <Link href={`/incident/${i.id}`} className="incident-card"><div><div className="pill-row"><span className={`badge ${i.severity}`}>{String(i.severity).toUpperCase()}</span><span className={`badge ${i.status}`}>{getShortStatusLabel(i.status)}</span><span className="badge">{i.stage||"Threat"} • {i.sub_stage||"Reported"}</span></div><h3>{i.title}</h3><p>{getPublicIncidentCode(i)} • {area?`${area.subRegion} / ${area.name}`:(i.city||"Location not classified")} • {formatDateShort(i.created_at)}</p></div><div style={{alignSelf:"center",color:"#1267e8",fontWeight:900}}>View →</div></Link>}
